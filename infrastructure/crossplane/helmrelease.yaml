apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: crossplane
  namespace: crossplane-system
spec:
  interval: 10m
  chart:
    spec:
      chart: crossplane
      version: 1.17.1
      sourceRef:
        kind: HelmRepository
        name: crossplane
        namespace: flux-system
  values:
    serviceAccount:
      customAnnotations:
        iam.gke.io/gcp-service-account: "fh-burgenland-crossplane@project-c8720d77-cb3f-4556-9fe.iam.gserviceaccount.com"
